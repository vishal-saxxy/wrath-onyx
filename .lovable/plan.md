# Hero enrichment pass

## Build
- Compress only the hero’s vertical composition so the complete answer card is visible at 1440×900 without scaling or changing copy.
- Align all four connector paths exactly to the answer card edge and remove floating endpoint dots.
- Apply the supplied paper, raised, dim, and hairline values while preserving ink and signal colours.
- Add a flat paper-dim visual field with a faint dot trace, then strengthen the answer-card depth and lightly lift the prompt.
- Add colour only to the AI answer header, source chips, engine dots, and missing-brand row as specified.
- Replace every existing ambient loop with one post-load, sequential travelling-dot loop that stops under reduced motion.

## Verification
- Check the hero in a real browser at 1440×900, including the fourth brand row and red status.
- Inspect connector/card geometry and confirm no floating dots.
- Measure requested text, label, signal, icon, and border contrast against the new neutral surfaces.
- Inspect live computed animations after the load sequence and confirm exactly one loop remains.
